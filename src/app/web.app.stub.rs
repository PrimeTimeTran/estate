/// Gentle reminder that RustDocs isn't perfect and links break
///
/// or get included in different build targets given the current implementation.
///
/// Check [src/app/app.web.rs] for the actual web implementation. (Zed Works. But doesn't "look like" a link)
/// Check [./app.web.rs] for the actual web implementation. (Zed Works. But doesn't "look like" a link)
/// Check [web](./app.web.rs) for the actual web implementation.  (Zed Works. But doesn't "look like" a link)
pub struct WebRuntime;
