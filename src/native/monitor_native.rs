// use std::path::Path;

// use notify::{Config, Event, EventKind, RecommendedWatcher, RecursiveMode, Watcher};
// use tokio::sync::mpsc;

// use crate::{app::event_channel, native::monitor};

// #[derive(Debug)]
// pub struct StateMonitor {
// 	watcher: RecommendedWatcher,
// 	rx: mpsc::Receiver<()>,
// }
